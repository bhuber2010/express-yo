

  <h1>{{title}}</h1>
  <br>
  {{#if none}}
    <h3>{{none}}</h3>
  {{/if}}
  <ul>
    {{#if dogs}}
    {{#each dogs}}
    <li><a href="/dogs/{{id}}">{{name}}</a> is a {{breed}} <button type="button" name="delete dog" title="Remove Dogger" data-id="{{id}}">Delete</button></li>
    {{/each}}
    {{/if}}
  </ul>
  <p><a href="/dogs/new">Add a doger</a></p>
  <br>
  <a href="/">Home</a>
  <a href="/auth/logout">Logout</a>
